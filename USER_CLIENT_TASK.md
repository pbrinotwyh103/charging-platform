你现在负责继续开发当前仓库中的“东软电动汽车充电桩应用管理平台”。

当前工作目录就是项目根目录，请首先完整阅读仓库结构、现有代码和 docs/reference 下的项目资料，不要立即修改代码。

我的姓名是史雨函。

====================
一、你的任务范围
====================

我只负责用户手机客户端模块。

用户端使用 Qt 框架模拟手机端交互体验，在 Linux 桌面环境运行。

我的主要代码范围是：

user-client/

包括但不限于：

user-client/controllers/
user-client/ui/
user-client/pages/
user-client/widgets/
user-client/resources/
user-client/map/

以及与用户客户端直接相关的测试。

除非为了让用户端正常编译确实不可避免，否则不要修改：

admin-client/
server/
database/

也不要擅自重构其他组员负责的模块。

如果发现用户端依赖的服务端接口尚未实现：
1. 不要擅自替其他成员实现完整服务端业务；
2. 明确记录缺失接口；
3. 在用户端按照设计说明书预留接口、状态和错误处理；
4. 能通过现有接口实现的功能继续实现；
5. 在最终报告中列出待联调接口。

====================
二、设计依据优先级
====================

严格按照以下优先级执行：

1. docs/reference/概要设计说明书
2. docs/reference/项目要求书
3. docs/reference/需求矩阵
4. docs/reference/分工.md
5. docs/reference/框架介绍.md
6. 当前仓库已有代码

如果文档之间存在冲突，不要自行猜测。

优先遵循概要设计说明书中的设计基线，同时记录冲突点。

====================
三、当前已经完成的内容
====================

当前项目已经能够：

1. 正常编译；
2. 启动服务端；
3. 启动用户客户端；
4. 用户使用 11 位手机号登录；
5. 首次手机号登录自动注册；
6. 登录后读取用户基本资料；
7. 客户端具备基础 TCP 通信、断线和重连能力。

当前使用 GCC 14 编译。

不要破坏上述已经能够运行的功能。

当前已经开始建立以下页面：

HomePage
ChargingPage
ProfilePage

请先检查它们目前的实际代码状态，不要假定此前修改已经全部正确。

====================
四、用户端最终目标
====================

按照概要设计说明书和需求矩阵，把我负责的用户客户端功能尽可能完整实现。

至少包括：

A. 用户账户与个人中心

- 手机号登录
- 首次登录自动注册
- 用户资料展示
- 头像选择
- PNG/JPEG 图片校验
- 头像尺寸检查和压缩
- 头像上传
- 昵称修改
- 昵称校验
- 钱包余额展示
- 模拟充值
- 充值记录展示
- 退出登录

B. 首页和附近充电站

- 区域选择
- 地址输入
- 模拟 GPS 定位
- 地址解析
- 当前经纬度维护
- 附近充电站查询
- 距离计算
- 按距离稳定排序
- 充电站卡片
- 站名
- 地址
- 电价
- 总桩数
- 空闲桩数
- 距离
- 收藏状态
- 加载状态
- 空数据状态
- 网络错误状态

C. 充电站详情

- 站点详情页面
- 站内充电桩列表
- 电桩编号
- 快充/慢充类型
- 功率
- 当前状态
- 空闲状态明显展示
- 收藏和取消收藏
- 预约入口

D. 腾讯地图相关功能

按照概要设计说明书要求实现：

- GCJ-02 坐标
- 地址解析
- 腾讯地图导航
- 驾车/步行方式选择
- QWebEngineView 页面
- HTTPS 白名单限制
- 地图接口不可用时的降级处理
- 没有 API 密钥时仍允许手动选择站点和距离排序

不要在源代码中提交真实私密 API 密钥。

API 密钥应该从配置或环境变量读取。

E. 预约和充电

- 进入充电页时首先查询当前活动订单
- 无活动订单时显示空状态
- 已预约状态
- 开始充电
- 充电中
- 停止中
- 对账中
- 待付款
- 已完成
- 异常结束

按照服务端真实状态展示，不允许客户端自行伪造业务终态。

F. 实时充电页面

展示：

- 当前状态
- 累计电量
- 实时功率
- 充电时长
- 当前费用
- 最后更新时间

按照设计说明：

- 使用 orderId 和 seq 处理快照；
- 丢弃旧快照；
- 10 秒没有新数据时显示“数据已过期”；
- 网络断开后保留最后一次有效数据显示，但明确提示已经不是实时状态。

G. 结算和异常

处理至少包括：

- 正常停止
- 余额不足
- 预约冲突
- 电桩不可用
- 设备故障
- 网络断开
- 请求超时
- 服务不可用
- 待付款
- 重新连接后的状态恢复

客户端不能因为网络断开自行判断充电已经结束。

====================
五、客户端架构要求
====================

按照概要设计说明书中的 M1 用户交互模块设计。

目标架构应体现：

页面控制器
ClientApi
SnapshotStore

UserMainWindow 只负责顶层窗口、登录态切换和主页面导航。

不要继续把全部业务代码堆在 usermainwindow.cpp 中。

推荐按照现有仓库情况逐步整理为：

user-client/
    controllers/
    api/
    stores/
    pages/
    widgets/
    map/
    resources/
    ui/

但不要为了目录形式进行无意义的大规模重构。

优先保证：

功能正确
代码职责清楚
能够编译
能够测试
能够联调

====================
六、界面要求
====================

这是模拟手机客户端，不是普通桌面后台软件。

保持纵向手机比例。

主界面建议采用：

首页
充电
我的

三个主要底部导航入口。

二级页面包括：

充电站详情
地图导航
资料编辑
充值记录
收藏列表
预约确认
订单结算

界面需要有基本的一致性和可用性。

使用 Qt Widgets。

可以使用 QSS 做统一样式。

不要把主要业务界面全部写成调试控件或简单 QLabel。

需要提供：

加载中
空数据
失败
成功
禁用
网络断开

等基本状态表现。

====================
七、开发方式
====================

不要一次性盲目修改全部代码。

首先执行以下阶段。

阶段 1：代码和需求审计

请完整检查：

- docs/reference 中的设计资料
- user-client
- common/protocol
- 与用户端相关的消息类型
- 当前测试
- 当前构建文件

输出：

1. 当前已经完成的用户端功能；
2. 当前缺失的用户端功能；
3. 服务端已经存在的用户接口；
4. 服务端尚不存在、会阻塞用户端的接口；
5. 建议的实现阶段；
6. 预计要新增和修改的文件。

先形成：

docs/user-client-implementation-plan.md

然后再开始开发。

阶段 2：用户端页面与架构

先完成：

- 页面拆分
- 主导航
- 页面控制
- ClientApi
- SnapshotStore
- 公共加载/错误状态

阶段 3：账户和钱包

完成：

- ProfilePage
- 昵称修改
- 头像
- 充值
- 充值记录

阶段 4：附近站点

完成：

- HomePage
- 地址输入
- 模拟定位
- 站点列表
- 距离
- 站点详情
- 收藏

阶段 5：地图导航

完成地图适配与导航页面。

阶段 6：预约、充电与结算

完成 ChargingPage 和完整用户端生命周期展示。

阶段 7：测试与整理

增加能够独立完成的用户端测试。

至少覆盖：

- 输入校验
- 页面状态切换
- 距离计算
- 快照序号处理
- 网络状态变化
- 关键 JSON 数据解析

====================
八、编译和测试
====================

每完成一个阶段都必须编译。

当前环境使用 GCC 14。

构建流程以仓库当前成功配置为准。

不要随意改回 GCC 15。

每次重要修改后：

1. 重新构建；
2. 执行已有 protocol_tests；
3. 执行已有 phase1_tests；
4. 执行新增加的用户端测试；
5. 如果测试失败，先修复再继续。

不要以“理论上能够工作”作为完成标准。

====================
九、Git 工作规则
====================

开始前：

1. 检查当前分支；
2. 检查 git status；
3. 不允许破坏 main；
4. 在我的用户端开发分支工作。

不要删除其他成员代码。

不要覆盖其他成员尚未提交的修改。

每完成一个独立阶段，可以形成一个清晰的提交。

提交信息应说明实际完成内容。

====================
十、禁止事项
====================

禁止：

- 擅自修改其他组员模块的大量业务代码；
- 用写死数据假装服务端功能已经实现；
- 将真实 API 密钥写入仓库；
- 为了让界面看起来完成而伪造订单终态；
- 网络断开就假定充电停止；
- 客户端直接访问 SQLite；
- 把费用计算和最终余额判定放到客户端；
- 为了减少报错而删除现有测试；
- 使用无法解释的大规模重构；
- 在没有运行测试的情况下宣布完成。

====================
十一、遇到缺失服务端接口时
====================

如果某项用户端功能需要的服务端协议尚不存在：

不要停下整个任务。

请：

1. 在 docs/user-client-integration-gaps.md 中记录：
   - 功能
   - 所需请求
   - 所需响应
   - 所需推送
   - 推荐字段
   - 当前缺失位置

2. 在客户端完成：
   - 页面
   - 数据模型
   - 控制逻辑
   - 加载状态
   - 错误处理
   - API 封装接口

3. 不要伪造“已经完成服务端联调”。

====================
十二、最终交付
====================

完成后请：

1. 确保项目可以重新构建；
2. 保证原有手机号登录功能没有回归；
3. 运行所有可运行测试；
4. 输出 git diff 摘要；
5. 输出实际完成的需求清单；
6. 输出因其他成员服务端尚未完成而无法最终联调的功能；
7. 输出我演示用户端时的操作步骤；
8. 输出后续与服务端成员联调需要确认的接口清单。

最终创建：

docs/user-client-progress.md

其中明确区分：

- 已完成并测试
- 已实现但待服务端联调
- 尚未完成
- 外部依赖阻塞

现在开始。

第一步不要直接修改代码。

先阅读项目、文档和当前 Git 状态，完成用户端审计和实施计划，然后向我汇报你准备如何推进。